# Screenshot Files

Generated evidence screenshots:

- `01_mlflow_ui.png`: local MLflow UI with many runs.
- `01b_mlflow_runs_metrics.png`: MLflow run metrics summary with accuracy/f1.
- `02_github_actions_status.png`: GitHub Actions run showing a successful pipeline.
- `03_api_output.png`: API `/health` and `/predict` output evidence.
- `04_gcs_objects.png`: GCS bucket object listing evidence.
- `06_bonus2_algorithm_runs.png`: MLflow evidence for multiple model algorithms.

Remaining manual UI screenshot needed for full submission:

- GCP Cloud Storage Console UI if the grader requires the web console rather than CLI evidence.
- DagsHub MLflow UI after DagsHub secrets are configured and the workflow runs.
